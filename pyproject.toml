[project]
name = "phase0-smoke"
version = "0.1.0"
description = "A program to read in csv file and convert to an sql table, then prints."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pandas>=3.0.3",
    "psycopg[binary]>=3.3.4",
]

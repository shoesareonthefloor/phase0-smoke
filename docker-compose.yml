services:
  db:
    image: postgres:17
    container_name: phase0-postgres
    environment:
      POSTGRES_USER: mrtro
      POSTGRES_PASSWORD: localdev
      POSTGRES_DB: sandbox
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: